{
  "name": "use_meme",
  "display_name": "表情包",
  "description": "Send a meme. action=search finds candidates by scene, mood, tags, or visual content; action=show sends one and renders it in the terminal (GIFs preview as a static frame). Search first unless the user gave an exact id.",
  "parameters": {
    "type": "object",
    "properties": {
      "action": {
        "type": "string",
        "enum": [
          "search",
          "show"
        ],
        "description": "search finds candidates, show sends."
      },
      "query": {
        "type": "string",
        "description": "search: scenario, mood, visual content, or intent."
      },
      "tags": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "search: preferred tags."
      },
      "id": {
        "type": "string",
        "description": "show: full sha256 id or a unique short id."
      },
      "size": {
        "type": "string",
        "description": "show: chafa size, e.g. 40x15."
      }
    },
    "required": [
      "action"
    ],
    "additionalProperties": false
  },
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "memes"
  ]
}
