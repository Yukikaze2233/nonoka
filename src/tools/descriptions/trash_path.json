{
  "name": "trash_path",
  "display_name": "移入回收站",
  "description": "Move paths to the system Trash instead of deleting permanently. Pass all paths in one call.",
  "parameters": {
    "type": "object",
    "properties": {
      "paths": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "minItems": 1,
        "description": "List of paths to move to the trash. Supports absolute paths, workspace-relative paths, and ~/ paths. When deleting multiple files, pass them all at once; do not split into multiple calls."
      }
    },
    "required": [
      "paths"
    ]
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "group",
  "groups": [
    "dev"
  ]
}
