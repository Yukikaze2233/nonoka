{
  "name": "create_goal",
  "display_name": "创建目标",
  "description": "Create a session-persistent goal. Use before genuinely long tasks; do not create one for trivial single-round work.",
  "parameters": {
    "type": "object",
    "properties": {
      "objective": {
        "type": "string",
        "description": "The concrete completion goal distilled from the human's request."
      },
      "max_goal_rounds": {
        "type": "integer",
        "description": "Optional: upper limit on automatic round continuations (positive integer). Uses the default if omitted."
      }
    },
    "required": [
      "objective"
    ],
    "additionalProperties": false
  },
  "permission": "writes",
  "always_loaded": false,
  "load_policy": "summary",
  "groups": [
    "planning"
  ]
}
