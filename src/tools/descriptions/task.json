{
  "name": "task",
  "display_name": "子代理",
  "description": "Delegate a task to a subagent that has your full tool set. Use it when the work would flood this conversation with tool output (reading many files, paging through search results, long command runs), when independent pieces can run in parallel, or for a one-off investigation unrelated to the main thread. The subagent starts with a fresh context and returns only one final message, so the prompt must be self-contained: background, conclusions you already confirmed, the goal, and what it should return. Use background=true for long tasks; you are woken automatically on completion.",
  "parameters": {
    "type": "object",
    "properties": {
      "description": {
        "type": "string",
        "description": "Short task description, used for progress display."
      },
      "prompt": {
        "type": "string",
        "description": "The complete task prompt for the subagent; it must stand on its own outside this conversation. The subagent cannot see your conversation history or anything the user said — include the background, constraints, and conclusions you have already found, and state explicitly what information it should return in its final reply."
      },
      "max_steps": {
        "type": "integer",
        "description": "Optional cap on tool-call steps. Unlimited by default; the run ends when the task completes."
      },
      "tier": {
        "type": "string",
        "enum": [
          "lite",
          "cheap",
          "standard",
          "flagship"
        ],
        "description": "Optional model tier by task difficulty: lite for trivial lookups and formatting, cheap for simple tool-using work, standard for regular multi-step work (default), flagship for hard reasoning. Every tier has the full tool set; an unconfigured tier falls back to the main model."
      },
      "background": {
        "type": "boolean",
        "description": "Run detached in the background: returns a job_id immediately, check progress with job(action=status), and you are woken automatically on completion."
      },
      "resume_id": {
        "type": "string",
        "description": "Optional. If the previous task failed due to a connection interruption and the error included a resume_id, pass it to resume from the last completed tool turn (valid only within this process)."
      }
    },
    "required": [
      "description",
      "prompt"
    ]
  },
  "permission": "writes",
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "agent"
  ],
  "timeout_seconds": 0
}
