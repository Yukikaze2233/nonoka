{
  "name": "present_artifact",
  "display_name": "预览文件",
  "description": "Present an existing completed deliverable file in the WebUI Artifact workspace. For reports, PDFs, images, and data files produced by commands or other tools. Prefer edit with an artifact: path for new text deliverables. Do not use it for routine source edits.",
  "parameters": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Path of the file to publish"
      },
      "title": {
        "type": "string",
        "description": "Optional display title"
      }
    },
    "required": [
      "path"
    ],
    "additionalProperties": false
  },
  "always_loaded": true,
  "load_policy": "summary",
  "groups": [
    "dev"
  ]
}
